
<div class="d-flex justify-content-around">
  <div class="ml-5 w-50">
    <h3 class="text-left"><%= @listing.name %></h3>
  </div>

</div>
  <div class="mr-5 w-50">
    <ul class="list-group">
      <h3>Specs:</h3>
      <% @listing.reviews.each do |dose| %>
      <li class= "list-group-item">
        <%=review.content %>
        <%= link_to "<i class='fas fa-trash'></i>".html_safe,
        review_path(review),
        method: :delete,
        data: { confirm: "Are you sure?" } %>
      </li>
      <% end %>
    </ul>
  </div>

